# Maite（リポジトリ名・識別子は `aipet` のまま）

Claude Code の作業ログから育つデスクトップ相棒（Electron + Node 標準モジュールのみ）。

**表に出る名前は Maite、コードの中の識別子は `aipet`。** ── mate（相棒）＋ AI ＋ aite（相手）。
`~/.aipet` / `AIPET_*` / KV バインディング / Worker 名 / リポジトリ名は**変えない**
（変えると、既存のデータ・クラウドの環境変数・スマホのブックマークが同時に死ぬ）。
名前を出すのは README・ドキュメント・`<title>`・`productName` だけ。

**作業を始める前に [docs/DESIGN.md](docs/DESIGN.md) を読むこと。** なぜその作りなのかが全部そこにある。ここに書くのは、読む前に壊してしまいがちな最低限だけ。

## 芯 ── ここを薄めたら作る意味がない

**戦闘力の出どころは作業ログ。** レベルもステータスもスキルも、Claude Code の使い方から生える。だから**育成のための操作を足さない**。餌やりボタン、デイリーミッション、戦闘中の操作 ── どれも「普段どおり仕事をするだけでいい」を壊す。

やらないと決めていること（DESIGN.md §3, §5）：

- **放置で弱る・死ぬ**。世話される側ではなく、見守ってくれる側に置く
- **戦闘に操作を要求する**。完全オートで、ログを眺めるだけ
- **通知でしゃべる**。黙っていられることが常駐アプリの品格
- **順位表を作る**。`state.json` は手元にあるので Lv999 は誰でも作れる。順位表が無ければ盛る動機も消える

## 触るときに壊しやすい 4 つ

### 1. `src/core/growth.js` が唯一の真実

成長ルールはここだけ。`applyEvent` は純関数（引数を書き換えない）で、`events.jsonl` を畳み込めばいつでも state を再構築できる。この性質があるから、ルール変更の移行が「作り直す」で済む。

- **renderer / mobile / Worker に成長ルールを再実装しない。** 二重定義は必ずズレる
- **サーバーも同じ `growth.js` を import する。** 別ルールが走ると PC とスマホで数字が食い違う
- **ルールを変えたら `STATE_VERSION` を上げる。** 上げると `loadState` が古い state を捨て、cursor を 0 に戻して全イベントを新ルールで畳み直す。上げ忘れると古いルールの産物が残り続ける
- **イベントは順番どおりに来ない。** 別のマシン・クラウド・過去ログの取り込みで、平気で昨日のぶんが今日の後ろに届く。だから畳み込みで「時刻が進んだ」前提を置かない ── 日付が変わったら日次 EXP をリセットするのは**進んだときだけ**（戻ったときにリセットしていて、上限 1,500 の日に 3,004 EXP 入っていた）、`lastEventAt` も戻さない（戻ると働いた直後に「5 時間ぶりのお土産」が出る）

**スキル・戦闘・型・装備・位は state に保存しない。** `skills.js` は traits から、`battle.js` はその一戦（2 時間ごと）の種から、`dungeon.js`（階層・装備・位）は traits と階の種から、`persona.js`（型）は traits と系統ベクトルから、毎回導出する純関数。だから閾値や戦闘の数字をいじっても `STATE_VERSION` を上げずに済む ── **保存に倒す前に、導出のままでいけないか考える**。戦闘は `Math.random()` を使わない（使うと再現できなくなり、Phase 2 のゴースト戦が成立しない）。乱数は `rng.js` の種付き乱数だけ。

**技の効き目の数字は `skills.js` の `SKILL_POWER` が唯一の出どころ。** `battle.js` もそこから読み、画面もそこから文を組む ── 前は `battle.js` が中に定数を抱えていて、画面には「素早さが上がる」としか出せなかった（効いているのか確かめようがない）。片方だけ動かすと、表示と挙動が黙ってずれる。

**なった職も実績に残る**（`job:<key>`）。ただし**まだなっていない職は一覧にも分母にも出さない** ── 出した瞬間に「16 個集める」チェックリストになる。

**保存するのは 2 つだけ ── 実績と、職ごとの経験値。** どちらも**いまの traits からは復元できない**。実績は「獲得した瞬間」が要るため（`achievements.js`）、職ごとの経験値（`state.jobExp`）は「どの職でいたときに稼いだか」が要るため ── 型は直近 2 週間の窓から出る導出値なので、過去のどの日に何だったかはもう残っていない。どちらも刻むのは畳んでいる途中のイベント時刻・その時点の型なので、**畳み直せば同じ結果が出る**（`applyEvent` の純粋性は保たれたまま）。職の経験値は**名乗れている（`settled`）ときだけ**貯める ── 3 回叩いただけの職が Lv2 で並ぶのを止めるため。

**実績を足すときは、それが目標になっていないか確かめる** ── 期限・連続日数・進捗バーを付けた瞬間にデイリーミッションになる（DESIGN.md §5b）。**うまくいかなかったほうの実績**（やらかし・空振り千本・働きすぎ・遠回り）も同じ縛りの中にある：狙って取れない量にする、責める言葉を使わない、作業を止める理由にしない。

### 2. hook の掟（`hooks/aipet-hook.mjs`）

Claude Code に**同期でブロックして**呼ばれる。おまけが本体を止めてはいけない。

1. **必ず exit 0**。非 0 は Claude Code の動作をブロックする
2. **待たない**。1 行 append して即終わる。成長計算はオーバーレイ側、送信は detached の別プロセス
3. **中身を保存しない**（下記）
4. **依存を持たない**。Node 標準モジュールだけの 1 枚もの ── クラウドのコンテナにこのファイルを置くだけで動く必要がある

3 つとも守れなくなったら、それは hook でやることではない。

### 3. プライバシー ── 記録するのは派生値だけ

`events.jsonl` に書くのはこの 1 行だけ：

```json
{"t":1786659596374,"e":"PostToolUse","s":"63528a31","p":"fa748169","tool":"Bash","ok":true}
```

`s` / `p` はセッション ID と cwd の **SHA-1 先頭 8 文字**（同一性判定にしか使わないので平文は不要）。

**保存しないもの** … プロンプト本文、ツールの引数、ファイルパス、コマンド、実行結果、コミットメッセージ、ホスト名。**フィールドを増やすときは DESIGN.md §2b を読んでから。** 外部送信は既定オフで、`endpoint` と `token` が両方揃って初めて動く（`src/core/config.js`）。

### 4. 同じ中身を持つファイルが 3 組ある

片方だけ直すとズレる。

| | |
|---|---|
| `hooks/aipet-hook.mjs` = `.claude/aipet-hook.mjs` | 中身が同一。このリポジトリをクラウドで開いたとき用のコピー |
| `cloud/settings.json` = `.claude/settings.json` | 中身が同一。クラウドの Claude Code に置く hook 設定 |
| `server/src/assets.js` | **自動生成。直接編集しない。** `npm run build:worker` で `src/renderer/style.css` `src/mobile/*` `src/shared/pet-svg.js` から作り直す |
| `server/src/worker.js` の `PAGE` ≒ `src/mobile/index.html` | HTML だけは自動生成に乗っていない手写し。**スマホのページに要素を足したら両方に足す**（CSS と JS は assets.js 経由で共有される） |

`src/shared/pet-svg.js`（キャラの絵）と `src/core/view.js`（表示用の変換）も、オーバーレイとスマホの共通点として置いてある。絵や変換を片側にコピーしない。

## 名前と型を触るとき

**名前は型から出る 1 つだけ**（DESIGN.md §8b）。`persona.js` が出した職がそのまま名前になり、
働き方が変われば名前のほうが変わる。肩書きを 2 つ並べない ── 前は「大工／学者／魔道書士」で、
どれが名前か読めなかった。

## 型（性格）を触るとき

`src/core/persona.js` の 4 軸は、**本文を一切読まずに**「どう使っているか」から出す
（DESIGN.md §8c）。ここに「プロンプトの中身を見れば精度が上がる」を持ち込むと、
hook をどのリポジトリにも置ける根拠そのものが消える ── **読まずに当てるから面白い**。

- **母数が足りないうちは名乗らない**（`settled`）。3 回叩いただけで断定したら、当たっていても嘘くさい
- **根拠（`from`）を必ず画面に出す。** 無いと占いになる
- **軸は直近 2 週間の窓（`state.days`）で見る。累計に戻さない** ── 累計だと分母が育って、一度決まった名前が二度と動かなくなる
- 境目の ±0.06 では累計に倒す（毎朝ちがう名前になるのを止めるため）。ここを外すと名前がぶれる
- 閾値は実測から置く。`scripts/usage.mjs` の分布を見ずに動かさない
- 軸を足すなら、**その値が人によって実際に散らばるか**を先に測る（立て直し率は誰でも 0.8 を超えて使い物にならなかった）

## しぐさを足すとき

**いちばん長く見ている画面は「何も起きていないオーバーレイ」**（DESIGN.md §5c）。だからここは賑やかにしていい ── ただし 3 つだけ守る。

1. **`calling`（許可待ち）では何もしない。** 返事を待っている子がのんきに伸びをしたら、見ている側が気づけない
2. **キーフレームは必ず `scale(var(--scale))` から書く。** 抜くと、しぐさに入った瞬間にレベルぶんの大きさが飛ぶ
3. **小道具の動きは、その場面のクラスの下に置く。** 素の `#prop-cat` に付けると、opacity を持つキーフレームが `.prop { opacity: 0 }` を上書きして、消えているはずの猫と小鳥と亀がいつも全部出る

**じゃれてくる相手（猫・小鳥・亀）は世話をするものではない。** 餌やりも撫でるボタンも付けない ── 向こうから来て、勝手に帰る。作業中と「打っている最中」には出さない（打っている最中に猫が来たら、それはただの邪魔）。

しぐさのクラス（`#stage.g-*　#creature`）は気分のアニメーションと同じ要素を奪うので、**CSS 上で気分の指定より後ろに置く**（特異度が同じなので後勝ち）。どれを出すかを決めるのは `src/shared/gestures.js` の純粋な関数で、ここだけはブラウザ無しでテストしている。

## 「1 日」と「一戦」を触るとき

日次 EXP 上限の区切り・夜更かし判定・**一戦の区切り（`BOUT_HOURS` = 2 時間）**は `src/core/clock.js` を通す。**機械のローカル時刻を直接見ない** ── Worker のローカルは UTC なので、`new Date(t).getHours()` を書いた瞬間に PC とサーバーで別の日になる（実際に「昼に働くほど夜目が育つ」が起きた）。オフセットは `applyEvent(state, event, { tzOffset })` で渡す。既定は機械のローカルなので PC 側の挙動は変わらない。

## Worker を触ったとき

`server/` と `src/core/` は **Cloudflare Worker に焼き込まれる**。リポジトリを取り込むだけでは本番は古いままなので、変えたら `server/` で `npx wrangler deploy` が要る。ローカルの常駐（`tools/watch.ps1 -Deploy`）はこの 2 つのパスの変更を見て自動で上げる ── **つまり `server/` か `src/core/` を触った push が、そのままデプロイの指示になる。**

**クラウドのセッションからは、既定では上げられない**（`api.cloudflare.com` が網で塞がれていて、鍵も無い）。設定が入っていない間でも、**上げたら通ることだけは確かめられる** ── `npx wrangler deploy --dry-run --outdir=/tmp/wr-out` は認証も外向きの通信も要らない。過去にここで `node:crypto` の混入を捕まえた（そのまま上げていたら本番が起動時に落ちていた）ので、手元で叩いてもらう前に必ず通す。

## Windows で動かすとき

**`state.json` の保存は転ける前提で書く。** Windows の rename は「開かれている宛先」に被せられないので、ウイルス対策・検索インデクサ・OneDrive の同期・もう 1 つ立ち上がったオーバーレイのどれかが同じ瞬間に触っていると **EPERM で落ちる**。実際、それだけで Electron の main プロセスが死んで画面いっぱいのエラーダイアログが出た。

`src/core/state.js` は「少し待って掛け直す → それでも駄目なら直接上書き → それも駄目なら false を返す」の 3 段で、**保存の失敗を例外として外に出さない**。`state.json` は `events.jsonl` から作り直せるただの控えなので、1 回書けなくても何も失われない ── **相棒のために本体を止めない**（hook の掟と同じ話）。ただし**書けなかったら cursor を進めない**（進めると、そのぶんのイベントが永久に畳まれない）。

main プロセスには `uncaughtException` / `unhandledRejection` の受けも置いてある。常駐のおまけが、ユーザーの作業を止めてはいけない。

## Windows 向けに `.ps1` を書くとき

**必ず UTF-8 BOM を付ける。** Windows PowerShell 5.1 は BOM の無い `.ps1` を ANSI（日本語環境では CP932）として読むので、日本語コメントが化けて壊れたバイト列が引用符を食い、**構文エラーで起動すらしない**。BOM があれば 5.1 でも UTF-8 として読むし、PowerShell 7 は元から UTF-8 扱いなので両方で動く。

`&&`・三項演算子・`??` も 5.1 には無い。**手順を書くときも 2 行に分ける** ── `cd server && npx wrangler deploy` は Windows PowerShell では動かない。**`cd` は毎回書く**（ブロックをまたぐと作業ディレクトリが読めない）。

**コードブロックの中には「打つコマンド」しか入れない。** 説明のための JS の断片やログの 1 行を ``` で囲むと、**そのまま PowerShell に貼られる** ── 実際に何度も起きた。コードでないものはコードブロックに入れない。

## コマンド

```sh
npm test                              # 成長・スキル・戦闘のテスト（依存ゼロ・Electron 不要）
npm start                             # オーバーレイ起動
npm run build:worker                  # assets.js を作り直す
node scripts/simulate.mjs scholar 5   # 学者として 5 日ぶんの作業ログを捏造
node scripts/simulate.mjs --live      # 1 秒おきに流す（気分の遷移を見る）
node scripts/simulate.mjs --reset     # 全部消す
node scripts/battle.mjs --level 20    # 戦闘ログをターミナルで読む
node scripts/balance.mjs              # 系統・レベル差・技の効きを実測する
node scripts/name.mjs さくら          # 名前を付け替える（既定は種から決まる職業名）
node scripts/status.mjs               # いまの状態をターミナルで読む（GUI 不要）
node scripts/send-log.mjs <file>      # 別の箱で溜まった記録を、ここから送る
node scripts/usage.mjs                # 手元の記録から実際の使われ方を測る
node scripts/license.mjs status       # 遡れる範囲（無料は直近 30 日）
node scripts/skin.mjs                 # 着替える（見た目だけ・数字に効かない）
node scripts/import.mjs               # 過去ログを遡って取り込む（--write で実行）
```

`AIPET_HOME` を指定すれば本番データに触らずに試せる：`AIPET_HOME=/tmp/aipet-test npm start`

**動作確認は `npm test` + `simulate.mjs` + `battle.mjs` + `status.mjs`。** Electron の GUI は環境によっては起動できないので、ロジックの検証はテストと捏造ログで完結させる。

## 現在地

Phase 0 / 1a / 1b は完了（hooks + 成長 + オーバーレイ + スマホビュー + Cloudflare Worker での合流）。

**Phase 1 は完了**（①スキル生成 ②オートバトル ③放置探索）。ほかに実績・しぐさ・系統相性・レベル上限 999・過去ログの取り込み・英語表示・ダンジョン（階層と装備）が入っている。

**そのあとに入れたぶん** ── 画面の日本語の揃え方、数字に注記を必ず添える決まり、技の効き目を数字で出すこと、位を 5 段（コモン〜レジェンド）にしたこと、職ごとのレベル、うまくいかなかったほうの称号、絵の差し替えの契約、そして **SVG に `innerHTML` を書かない**（iPhone だけ節の中身が消えていた）。

**ダンジョンと装備も導出**（`src/core/dungeon.js`）。到達階層は作業量（`traits`）から、拾い物は「個体の種 + 階」から毎回引き直す。装備は**枠ごとに一番強いものが勝手に乗る** ── 選ばせない（選ばせた瞬間、良いものを狙って潜る話になる）。装備がステータスに乗るぶんは、練習相手にも同じだけ持たせる（`bots.js`）── 合わせないと、潜るほど一方的に勝つだけになる。技も同じ扱いで、**練習相手は「いまの自分と同じくらい育った相手」**。

**戦闘は「読むログ」から「たまに見かけるしぐさ」に降ろした**（DESIGN.md §5c）。この判断で §7 の Phase 2 の基準もずれている ── ゴースト戦の値打ちは戦闘の中身ではなく「知らない誰かの個体が出てくること」のほう。まだ判断は出ていないので、Phase 2 の実装に踏み込まない。

**スキンは見た目だけ**（`src/core/skins.js`、DESIGN.md §6c）。色・模様・小物の 3 枚しか触らず、**顔つきには一切触らない** ── 目つきと口元は型が決めるもので、そこを買えるようにすると「働き方が顔に出る」がお金で上書きできる。テストで縛ってある。

**課金の方針は §6b に確定済み**：売るのは「遡れる範囲」であって EXP の量ではない。同じログを入れたら同じレベルが出る、を崩さない。実装は `src/core/license.js`（Ed25519 の署名を検証するだけ）で、**課金で変わるのは `reachFor()` が返す日数ひとつ**。ここに係数や倍率を足さない ── 足した瞬間、公開して困るものが生まれる。

**鍵は 1 本 30 日ぶんで、重ねて買える**（`REACH_DAYS_PER_KEY`）。「1 ドルで 30 日ぶんの経験値」と言ってよいが、それは**その 30 日に本当にやった作業ぶん**であって、金額で配られる EXP ではない ── 1 ヶ月休んでいた人が払っても増えるのは 0。ここを混ぜた売り文句を書かない。

**`PUBLIC_KEYS` は空のまま配ってよい。** 空＝「課金の口がまだ開いていない」で、全員が無料の範囲で動く。売り始めるときに `scripts/license.mjs init` で対を作り、**公開鍵だけ**を貼る（秘密鍵はリポジトリの外に出る）。**催促しない** ── 範囲外に何件残っているかは `import.mjs` を叩いたときの 1 行だけで、オーバーレイにもスマホにも出さない。

**バランスの数字は実測に寄せる。** `scripts/usage.mjs` が `~/.claude/projects` から本物の分布を出す。日次上限 3,000 も実測から決めた ── 400 のときは 1.7 時間で埋まり、1,500 でも「12.6 時間・ツール 1,269 回の日」で **9 時間目に上限へ届いて、その日の 53% を捨てていた**（1 日まるごと働いて Lv7 で止まる）。勘で動かさない。手元の `events.jsonl` で「素の EXP」と「上限に届く時刻」を測ってから動かす。

**セッションは ID では数えない。** クラウドの Claude Code はコンテナが生きている限り同じ ID を使い続けるので、実測で **12.6 時間・SessionStart 22 回・プロンプト 78 件がセッション 1 本**になっていた。数えるのは「席に着いた回数」── 新しい ID か、同じ ID で開き直されたか（`SessionStart`）、45 分空けて戻ってきたか。**開いただけでは数えない**（IDE 拡張が裏で立てる 0.5 秒のセッションを弾くため、数えるのは最初のプロンプトが来た時点）。

**戦闘まわりの数字を触ったら `scripts/balance.mjs` を通す。** 系統ごとの勝率・レベル差の効き方・技ごとの寄与・潜る深さと装備の伸びが出る。ここで守っている線は 4 つ：**同レベルなら系統ごとの平均勝率は 45〜55%**（配点は合計ではなく勝率で揃える ── atk 1 点は def 1 点の 2.5 倍の値打ちがあるので、合計を揃えると防御系統が最初から負ける）、**レベル差の効き方はどの高さでも同じ形**（差は段数ではなく比で見る）、**技 1 つの寄与は +25pt 未満**、**実際に育てた個体の勝率は 45〜65% で、連敗は丸 1 日（12 戦）以内**。

未決のままの論点（プロジェクトごとに別個体にするか、タスクトレイ、KV のログ上限、スプライトのレイヤー化、「検証の一閃」のために hook を踏み込ませるか、戦績を保存するか、レベル曲線の指数、比率を窓方式にするか）は DESIGN.md §9。**勝手に決めない。**（「拾ったものを貯めるか」は決着 ── 貯めずに導出で済んだ）
